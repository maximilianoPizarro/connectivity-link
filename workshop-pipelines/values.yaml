# Values file for workshop-pipelines Helm chart
# This file customizes the installation from the workshop-pipelines Helm repository
# Repository: https://maximilianopizarro.github.io/workshop-pipelines/

# Chart name and version (adjust as needed)
# chart: workshop-pipelines
# version: 

# Add your custom values below
# Example structure (adjust based on the actual chart values):
# replicaCount: 1
# image:
#   repository: 
#   tag: 
#   pullPolicy: IfNotPresent
# service:
#   type: ClusterIP
#   port: 8080
# ingress:
#   enabled: false

# Add any other chart-specific values here

