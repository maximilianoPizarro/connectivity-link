# OAuth HTPasswd: Secret in openshift-config + OAuth cluster (cluster-scoped).
# Do not set namespace here so OAuth stays cluster-scoped and Secret keeps its own namespace.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - htpasswd-secret.yaml
  - oauth-cluster-htpasswd.yaml
