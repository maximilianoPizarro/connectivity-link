# Deploys the Kuadrant CR to kuadrant-system (recommended namespace for the CR).
# RHCL operator creates Limitador/Authorino per topology; if they land in kuadrant-system,
# ensure kuadrant-ratelimit-service is in the same namespace or Istio WasmPlugin points to it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # OCP ARM
  #- limitador.yaml
  #- authorino.yaml
  - kuadrant.yaml
namespace: kuadrant-system
