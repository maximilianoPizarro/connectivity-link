apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #- keycloak-authpolicy.yaml
  - rolebinding.yaml
  - oidc-policy.yaml
  - gateway-neuralbank.yaml
  - neuralbank-route.yaml
  - ratelimit-policy-customers.yaml

# Gateway in istio-system; Route + HTTPRoutes + OIDCPolicy + RateLimitPolicy in neuralbank-stack (policies same ns as target). rolebinding in neuralbank-stack.
patches: []
