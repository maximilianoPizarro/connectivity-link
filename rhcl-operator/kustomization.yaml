apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #- keycloak-authpolicy.yaml
  #- neuralbank-oidc-callback.yaml
  - rolebinding.yaml
  - oidc-policy.yaml
  # Kuadrant CR is deployed to kuadrant-system via rhcl-operator/kuadrant-system app
  - neuralbank-route.yaml
  - ratelimit-policy-customers.yaml

namespace: neuralbank-stack
