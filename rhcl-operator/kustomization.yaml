apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  #- keycloak-authpolicy.yaml
  #- neuralbank-oidc-callback.yaml
  - rolebinding.yaml
  - oidc-policy.yaml
  - gateway-neuralbank.yaml
  - neuralbank-route.yaml
  - ratelimit-policy-customers.yaml

# Gateway + Route + HTTPRoutes in neuralbank-stack. OIDCPolicy, RateLimitPolicy in kuadrant-system (target HTTPRoutes). rolebinding (gateways+httproutes) in neuralbank-stack. No global namespace.
patches: []
